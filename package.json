{
  "name": "sensorbird",
  "homepage": "https://hanglma.github.io/sensorbird",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "fs": "^0.0.1-security",
    "gh-pages": "^6.1.1",
    "path": "^0.12.7",
    "typescript": "^5.2.2",
    "vite": "^5.1.3"
  },
  "dependencies": {
    "throttle-typescript": "^1.1.0"
  }
}
